# PHP rulepacks

This directory is reserved for declarative and versioned PHP rulepacks.

The bootstrap rules are currently native Rust rules so the engine contract can stabilize first. Declarative schemas will be introduced in a later increment.
